#include <unordered_set>
#include <vector>

namespace utils {
    void GeneratePredicates(std::vector<std::string>, std::string);
    std::unordered_set<std::string> PopulateVariables(std::string);
}
